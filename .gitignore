/target

# What `perch holdings export` writes, in whatever directory the command was
# typed in — which may well be this one. An Export holds a working Credential
# for every Account the machine has, so it is one `git add -A` away from a
# public history.
*.age
