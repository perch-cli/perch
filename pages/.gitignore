# The site's own, so nothing at the root of the repository has to know that one
# directory under it is a JavaScript project (ADR 0062).
node_modules/

# What `astro build` writes, and what the Pages workflow uploads.
dist/

# Astro's generated types and content cache.
.astro/
