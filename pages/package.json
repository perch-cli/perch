{
  "name": "perch-site",
  "private": true,
  "description": "https://perch-cli.github.io/perch/ — the front page, the installers and the guide (ADR one-thing-renders-the-site)",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro sync && tsc --noEmit && oxlint && oxfmt --check"
  },
  "dependencies": {
    "@astrojs/markdown-satteri": "^0.3.5",
    "@astrojs/starlight": "^0.41.7",
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "oxfmt": "^0.63.0",
    "oxlint": "^1.78.0",
    "typescript": "^7.0.2"
  },
  "packageManager": "pnpm@11.22.0"
}
